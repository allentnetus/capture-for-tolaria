{
  "name": "@capture-for-tolaria/helper",
  "version": "0.1.0-beta.2",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@capture-for-tolaria/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "esbuild": "^0.28.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  }
}
