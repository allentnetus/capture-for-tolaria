{
  "name": "@capture-for-tolaria/extension",
  "version": "0.1.0-beta.6",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@capture-for-tolaria/extractor": "workspace:*",
    "@capture-for-tolaria/markdown": "workspace:*",
    "@capture-for-tolaria/protocol": "workspace:*",
    "@capture-for-tolaria/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.28.2",
    "jsdom": "^26.1.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  }
}
